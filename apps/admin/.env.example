# Point at the deployed API (or http://localhost:4000 for local dev).
VITE_API_URL=http://localhost:4000

# Same Supabase project as apps/mobile — the publishable anon key is safe
# in a client bundle by design; it's what enforces auth, not a secret.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
